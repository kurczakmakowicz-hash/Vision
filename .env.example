# Copy this file to `.env` and fill in your keys. `.env` is git-ignored — never
# commit real keys. Vision loads this at startup via python-dotenv.

# Required from Tier 1 (the brain):
ANTHROPIC_API_KEY=

# Required from Tier 3 (voice):
DEEPGRAM_API_KEY=
ELEVENLABS_API_KEY=
